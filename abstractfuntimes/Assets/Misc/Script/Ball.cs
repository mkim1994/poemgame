using UnityEngine;
using System.Collections;

public class Ball : MonoBehaviour {


	//Animation animation = GetComponent<Animation>();
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		/*if(Input.GetKey ("w")){
			animation.Play("balltest");
		}*/
	}
}
